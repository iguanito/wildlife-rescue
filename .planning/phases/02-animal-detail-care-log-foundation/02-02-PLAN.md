---
phase: 02-animal-detail-care-log-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - api/tests/care-log.test.js
  - api/tests/animal-detail.test.js
autonomous: true
requirements:
  - CARE-01
  - CARE-02
  - CARE-03
  - DETAIL-01
  - DETAIL-02

must_haves:
  truths:
    - "api/tests/care-log.test.js exists and contains failing stubs for CARE-01, CARE-02, CARE-03"
    - "api/tests/animal-detail.test.js exists and contains failing stubs for DETAIL-01, DETAIL-02"
    - "Running test stubs produces test failures (red), not syntax errors or crashes"
    - "Test files use the established supertest + db helpers pattern from auth.test.js"
  artifacts:
    - path: "api/tests/care-log.test.js"
      provides: "Integration test stubs for POST and GET /carelogs routes"
      contains: "CARE-01"
    - path: "api/tests/animal-detail.test.js"
      provides: "Integration test stubs for animal detail GET endpoint"
      contains: "DETAIL-01"
  key_links:
    - from: "api/tests/care-log.test.js"
      to: "api/tests/helpers/db.js"
      via: "require('./helpers/db')"
      pattern: "require.*helpers/db"
    - from: "api/tests/care-log.test.js"
      to: "api/tests/helpers/fixtures.js"
      via: "require('./helpers/fixtures')"
      pattern: "require.*helpers/fixtures"
---

<objective>
Create Wave 0 test stub files so Plan 01 has automated verification commands to run. Both files must exist with correct structure before Plan 01 executor runs tests.

Purpose: Nyquist rule — every task with an automated verify command needs the test file to exist. These stubs are RED (failing) until Plan 01 implements the backend routes, then GREEN after.

Output: api/tests/care-log.test.js (stubs for CARE-01/02/03), api/tests/animal-detail.test.js (stubs for DETAIL-01/02).
</objective>

<execution_context>
@/home/arnaud/workspace/perso/wildlife-rescue/.claude/get-shit-done/workflows/execute-plan.md
@/home/arnaud/workspace/perso/wildlife-rescue/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-animal-detail-care-log-foundation/02-CONTEXT.md
@.planning/phases/02-animal-detail-care-log-foundation/02-VALIDATION.md
</context>

<interfaces>
<!-- Existing test infrastructure to replicate exactly. -->

From api/tests/auth.test.js (the boilerplate pattern all test files follow):
```javascript
const request = require('supertest');
const { connectTestDB, clearTestDB, closeTestDB } = require('./helpers/db');
const { createTestUser, TEST_PASSWORDS } = require('./helpers/fixtures');

let app;

beforeAll(async () => {
  await connectTestDB();
  app = require('../index');
});

afterEach(async () => {
  await clearTestDB();
});

afterAll(async () => {
  await closeTestDB();
});
```

From api/tests/helpers/fixtures.js (createTestUser signature):
```javascript
async function createTestUser(role) {
  // Creates user with email `test-${role}-${Date.now()}@example.com`
  // Returns saved User document with _id
}
// TEST_PASSWORDS.plain = 'TestPassword123!'
```

Login helper pattern (from access.test.js) to get a cookie for authenticated requests:
```javascript
async function login(role) {
  const user = await createTestUser(role);
  const res = await request(app)
    .post('/api/auth/login')
    .send({ email: user.email, password: TEST_PASSWORDS.plain });
  return res.headers['set-cookie'][0].split(';')[0]; // "token=..."
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create care-log.test.js with stubs for CARE-01, CARE-02, CARE-03</name>
  <files>api/tests/care-log.test.js</files>

  <read_first>
    - api/tests/auth.test.js — boilerplate pattern (beforeAll, afterEach, afterAll, request, helpers)
    - api/tests/access.test.js — login helper pattern using createTestUser + POST /api/auth/login
    - api/tests/helpers/fixtures.js — createTestUser(role) signature and TEST_PASSWORDS
    - api/tests/helpers/db.js — verify connectTestDB/clearTestDB/closeTestDB exports
    - .planning/phases/02-animal-detail-care-log-foundation/02-VALIDATION.md — exact test IDs and commands
  </read_first>

  <action>
    Create api/tests/care-log.test.js with the following structure:

    1. Standard boilerplate (copy from auth.test.js pattern): require supertest, db helpers, fixtures; beforeAll connect + require app; afterEach clear; afterAll close.

    2. Helper function `login(role)` that calls createTestUser(role) then POST /api/auth/login and returns the cookie string (split on ';')[0].

    3. Helper function `createAnimal(cookie)` that POSTs to /api/animals with a minimal valid body and returns the created animal's _id. Minimal body:
       { name: 'Test Fox', species: 'Red Fox', intakeDate: new Date().toISOString() }

    4. describe('CARE-01: Staff can add care log entry') — one test:
       - test name: 'care.create — POST /api/animals/:id/carelogs with staff token creates care log and returns 201'
       - Login as staff, create animal, POST to /api/animals/:animalId/carelogs with body:
         { date: new Date().toISOString(), type: 'feeding', notes: 'Fed well' }
       - expect(res.status).toBe(201)
       - expect(res.body.type).toBe('feeding')
       - expect(res.body.notes).toBe('Fed well')

    5. describe('CARE-01: Volunteer cannot add care log entry') — one test:
       - test name: 'care.create.forbidden — POST /api/animals/:id/carelogs with volunteer token returns 403'
       - Login as volunteer, create animal using staff login separately
       - POST to /api/animals/:animalId/carelogs with volunteer cookie
       - expect(res.status).toBe(403)

    6. describe('CARE-02: Care log includes createdBy attribution') — one test:
       - test name: 'care.attribution — POST /api/animals/:id/carelogs sets createdBy to logged-in user'
       - Login as staff, create animal, POST care log
       - expect(res.body.createdBy).toBeDefined()
       - expect(res.body.createdBy.email).toBe(staffUser.email)
       - expect(res.body.createdBy.role).toBe('staff')
       - expect(res.body.date).toBeDefined()   // activity date
       - expect(res.body.submittedAt).toBeDefined()  // submission date

    7. describe('CARE-03: Staff can view all care logs for animal') — one test:
       - test name: 'care.query — GET /api/animals/:id/carelogs returns array of care logs sorted by date desc'
       - Login as staff, create animal, POST two care logs with different dates (one older, one newer)
       - GET /api/animals/:animalId/carelogs
       - expect(res.status).toBe(200)
       - expect(Array.isArray(res.body)).toBe(true)
       - expect(res.body.length).toBe(2)
       - Verify first entry date >= second entry date (sorted descending):
         expect(new Date(res.body[0].date) >= new Date(res.body[1].date)).toBe(true)

    CRITICAL: At time of file creation, api/models/CareLog.js does NOT yet exist (it's created in Plan 01 Task 1). Tests will fail with "Cannot find module" errors until Plan 01 runs. This is the correct RED state.
    CRITICAL: Do not use watch mode flags. All test commands are single-run.
    CRITICAL: Single quotes for strings, 2-space indentation — match existing test file style.
  </action>

  <verify>
    <automated>node -e "require('./api/tests/care-log.test.js')" 2>&1 | head -5 || true</automated>
  </verify>

  <acceptance_criteria>
    - File api/tests/care-log.test.js exists
    - grep -c "describe.*CARE-0" api/tests/care-log.test.js returns 3 (one per requirement)
    - grep -n "toBe(201)" api/tests/care-log.test.js shows at least 1 line (CARE-01 assertion)
    - grep -n "toBe(403)" api/tests/care-log.test.js shows at least 1 line (volunteer rejection)
    - grep -n "createdBy" api/tests/care-log.test.js shows CARE-02 assertions
    - grep -n "submittedAt" api/tests/care-log.test.js shows the attribution field check
    - grep -n "connectTestDB\|clearTestDB\|closeTestDB" api/tests/care-log.test.js shows all three helpers imported
    - grep -n "require.*supertest" api/tests/care-log.test.js shows supertest import
  </acceptance_criteria>

  <done>care-log.test.js exists with 4 integration tests covering CARE-01 (create + forbidden), CARE-02 (attribution), CARE-03 (query+sort). File has correct boilerplate structure.</done>
</task>

<task type="auto">
  <name>Task 2: Create animal-detail.test.js with stubs for DETAIL-01 and DETAIL-02</name>
  <files>api/tests/animal-detail.test.js</files>

  <read_first>
    - api/tests/auth.test.js — boilerplate pattern to replicate
    - api/tests/care-log.test.js — just created; reuse login() and createAnimal() pattern
    - api/models/Animal.js — see what fields Animal schema has for DETAIL-02 assertions
  </read_first>

  <action>
    Create api/tests/animal-detail.test.js with the following structure:

    1. Standard boilerplate (same as care-log.test.js): supertest, db helpers, fixtures; beforeAll/afterEach/afterAll lifecycle.

    2. Same login(role) helper as care-log.test.js.

    3. describe('DETAIL-01: Timeline merges intake, care logs, medical records chronologically') — two tests:

       Test A: 'detail.timeline.carelogs — GET /api/animals/:id/carelogs returns logs in date order after multiple creates'
       - Login as staff, create animal
       - POST two care logs: one with date 2 days ago, one with date yesterday
       - GET /api/animals/:animalId/carelogs
       - expect(res.status).toBe(200)
       - expect(res.body.length).toBe(2)
       - Verify descending order: expect(new Date(res.body[0].date) >= new Date(res.body[1].date)).toBe(true)

       Test B: 'detail.timeline.medical — GET /api/animals/:id/medical returns medical records'
       - Login as vet, create animal
       - POST to /api/animals/:animalId/medical with body: { description: 'Checkup', date: new Date().toISOString() }
       - GET /api/animals/:animalId/medical
       - expect(res.status).toBe(200)
       - expect(Array.isArray(res.body)).toBe(true)
       - expect(res.body[0].description).toBe('Checkup')

    4. describe('DETAIL-02: Detail page shows all animal fields') — one test:

       Test: 'detail.fields — GET /api/animals/:id returns all key intake fields'
       - Login as staff, create animal with full body:
         { name: 'Test Hedgehog', species: 'European Hedgehog', intakeDate: '2026-03-01', status: 'in-center', notes: 'Found near road' }
       - GET /api/animals/:animalId (no carelogs route — just the animal)
       - expect(res.status).toBe(200)
       - expect(res.body.name).toBe('Test Hedgehog')
       - expect(res.body.species).toBe('European Hedgehog')
       - expect(res.body.status).toBeDefined()
       - expect(res.body.intakeDate).toBeDefined()
       - expect(res.body.notes).toBe('Found near road')
       - expect(res.body._id).toBeDefined()

    CRITICAL: DETAIL-01 is about the API data being available for frontend merging — not frontend rendering. The integration tests validate the data sources; visual timeline merge is verified manually per VALIDATION.md.
    CRITICAL: Do not add tests for routes that do not exist yet. Only test routes that exist: GET /api/animals/:id, GET /api/animals/:id/carelogs, GET /api/animals/:id/medical, POST /api/animals/:id/carelogs, POST /api/animals/:id/medical.
  </action>

  <verify>
    <automated>node -e "require('./api/tests/animal-detail.test.js')" 2>&1 | head -5 || true</automated>
  </verify>

  <acceptance_criteria>
    - File api/tests/animal-detail.test.js exists
    - grep -c "describe.*DETAIL-0" api/tests/animal-detail.test.js returns 2 (one per requirement)
    - grep -n "DETAIL-01" api/tests/animal-detail.test.js shows the requirement label in describe block
    - grep -n "DETAIL-02" api/tests/animal-detail.test.js shows the requirement label in describe block
    - grep -n "res.body.name" api/tests/animal-detail.test.js shows DETAIL-02 field assertion
    - grep -n "res.body.intakeDate" api/tests/animal-detail.test.js shows intake date assertion
    - grep -n "connectTestDB\|clearTestDB\|closeTestDB" api/tests/animal-detail.test.js shows all three helpers
    - grep -n "require.*supertest" api/tests/animal-detail.test.js shows supertest import
  </acceptance_criteria>

  <done>animal-detail.test.js exists with 3 integration tests covering DETAIL-01 (timeline data sources) and DETAIL-02 (all animal fields). File has correct boilerplate structure matching existing test files.</done>
</task>

</tasks>

<verification>
Verify both test files parse without syntax errors:
node --check /home/arnaud/workspace/perso/wildlife-rescue/api/tests/care-log.test.js
node --check /home/arnaud/workspace/perso/wildlife-rescue/api/tests/animal-detail.test.js

Both should exit 0 (syntax valid, even if tests will fail at runtime until Plan 01 completes).
</verification>

<success_criteria>
- api/tests/care-log.test.js exists with 4 tests covering CARE-01/02/03
- api/tests/animal-detail.test.js exists with 3 tests covering DETAIL-01/02
- Both files have correct boilerplate (connectTestDB, clearTestDB, closeTestDB lifecycle)
- Both files are syntactically valid (node --check passes)
- Test file structure matches auth.test.js/access.test.js conventions
</success_criteria>

<output>
After completion, create .planning/phases/02-animal-detail-care-log-foundation/02-02-SUMMARY.md
</output>
